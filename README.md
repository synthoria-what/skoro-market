# СкороМаркет

Курсовой проект: витрина продуктовой доставки с каталогом, корзиной, регистрацией и админ-панелью.

## Как запустить

Откройте файл `index.html` двойным кликом.

## Что внутри

- `index.html` - главная страница сайта.
- `app.js` - логика каталога, регистрации, корзины, заказов и админ-панели.
- `styles.css` - оформление сайта.
- `assets/` - локальные фотографии товаров.

## Сохранение данных

Данные остаются на этом компьютере. Сохраняются:

- зарегистрированные пользователи;
- товары каталога;
- изменения из админ-панели;
- корзина и текущий пользователь.

## Админ-панель

Админ-панель открывается кнопкой `Админ` в шапке сайта или через адрес:

```text
index.html#admin
```

В ней можно добавлять, редактировать и удалять товары. Для фото указывается локальный путь, например:

```text
assets/dj-milk.webp
```
